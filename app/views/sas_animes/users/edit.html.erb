<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center py-4">
    <div>
        <h2 class="h4">Editar Usuário</h2>
    </div>
</div>

<%= render 'form', {user: @user, url: sas_animes_user_url(@user), method: :patch} %>
